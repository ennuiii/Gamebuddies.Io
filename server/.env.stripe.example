# Stripe Configuration
# Get these from https://dashboard.stripe.com/test/apikeys

# API Keys (use TEST keys for development)
STRIPE_SECRET_KEY=sk_test_YOUR_SECRET_KEY_HERE
STRIPE_PUBLISHABLE_KEY=pk_test_YOUR_PUBLISHABLE_KEY_HERE

# Webhook Secret (optional - only needed for webhooks)
# Get from Stripe CLI: stripe listen --forward-to localhost:3033/api/stripe/webhook
STRIPE_WEBHOOK_SECRET=whsec_YOUR_WEBHOOK_SECRET_HERE

# Client URL (for redirects after payment)
CLIENT_URL=http://localhost:3000
# For production: CLIENT_URL=https://gamebuddies.io

# ============================================================================
# IMPORTANT: Price IDs are NO LONGER needed in .env!
# ============================================================================
# Prices are now fetched automatically from your Stripe account via API.
#
# The system automatically identifies your prices based on:
# 1. Product name containing "lifetime", "premium", "monthly", "pro", or "subscription"
# 2. Payment type (one-time vs recurring monthly)
# 3. Optional metadata tags: { tier: "lifetime" } or { tier: "monthly" }
#
# Just create your products in Stripe Dashboard with appropriate names:
# - "Lifetime Premium" (one-time payment)
# - "Monthly Pro" (recurring monthly subscription)
# ============================================================================
